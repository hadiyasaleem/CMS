*.iml
.gradle
/local.properties
/.idea/
.DS_Store

# Legacy pre-rewrite single-app module — superseded by the mobile-*/desktop-* multi-module
# structure, kept on disk for reference only, never tracked.
/app/
/build
**/build/
!Documentation/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase service account keys — never commit these
*firebase-adminsdk*.json
serviceAccountKey.json

# Node (seed scripts)
node_modules/
