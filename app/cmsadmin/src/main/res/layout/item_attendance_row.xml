<?xml version="1.0" encoding="utf-8"?>
<!--
    item_attendance_row.xml
    One student row in the attendance table RecyclerView.

    Layout (all horizontal inside a single LinearLayout):
      [student name + roll]  |  [date cells — added dynamically]  |  [summary %]

    The container_dates LinearLayout is populated at bind time by
    AttendanceTableAdapter so the column count matches the date headers.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:background="@color/surface_container_lowest">

    <!-- Fixed-width student identity column -->
    <LinearLayout
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="8dp">

        <TextView
            android:id="@+id/tv_student_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Student Name"
            android:textSize="13sp"
            android:textColor="@color/primary"
            android:fontFamily="@font/public_sans"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/tv_roll_no"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ROLL NO: XX-000"
            android:textSize="9sp"
            android:letterSpacing="0.08"
            android:textColor="@color/on_surface_variant"
            android:fontFamily="@font/public_sans"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- Dynamic date status cells — populated by AttendanceTableAdapter -->
    <LinearLayout
        android:id="@+id/container_dates"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical" />

    <!-- Summary percentage — fixed right column -->
    <TextView
        android:id="@+id/tv_summary"
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:text="100.0%"
        android:textSize="12sp"
        android:textColor="@color/primary"
        android:fontFamily="@font/public_sans"
        android:textStyle="bold"
        android:gravity="end"
        android:paddingEnd="16dp" />

</LinearLayout>